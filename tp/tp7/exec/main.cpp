
#define F_CPU 8000000
#include <util/delay.h>
#include <avr/io.h>
#include "Uart.h"
#include "Timer.h"

int main() {
    // stdout = uart.getEmulatedFile();
    // stdin = uart.getEmulatedFile();
    // while (true)
    // {

    //     printf("%c\n", getchar());
    // }
    
    return 0;
}